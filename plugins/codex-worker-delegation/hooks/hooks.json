{
  "description": "Enforces AUTO / DELEGATE / MAIN policy at Codex PreToolUse, including provider-aware native-subagent and delegation boundaries.",
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${PLUGIN_ROOT}/hooks/run-policy.sh\"",
            "timeout": 5,
            "statusMessage": "Applying delegation policy"
          }
        ]
      }
    ]
  }
}
