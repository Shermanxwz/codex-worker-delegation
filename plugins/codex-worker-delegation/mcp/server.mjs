#!/usr/bin/env node
import fs from 'node:fs/promises'; import os from 'node:os'; import path from 'node:path';
let buffer=''; process.stdin.setEncoding('utf8'); process.stdin.on('data',(c)=>{buffer+=c;drain()});
function drain(){while(true){const i=buffer.indexOf('\n');if(i<0)return;const line=buffer.slice(0,i).trim();buffer=buffer.slice(i+1);if(line)handle(JSON.parse(line)).catch(e=>reply(null,null,{code:-32603,message:e.message}))}}
async function handle(msg){if(msg.method==='initialize')return reply(msg.id,{protocolVersion:msg.params?.protocolVersion||'2025-06-18',capabilities:{tools:{}},serverInfo:{name:'codex-worker-delegation',version:'1.0.0'}});if(msg.method==='notifications/initialized')return;if(msg.method==='tools/list')return reply(msg.id,{tools:[{name:'delegation_status',description:'Read current user-controlled delegation mode and non-secret third-party routing status.',inputSchema:{type:'object',properties:{},additionalProperties:false}}]});if(msg.method==='tools/call'&&msg.params?.name==='delegation_status'){const s=await state();const safe={mode:s.mode,mainSource:s.mainSource,models:s.models,provider:s.provider&&{name:s.provider.name,baseUrl:s.provider.baseUrl,protocol:s.provider.protocol},protocolCache:s.protocolCache};return reply(msg.id,{content:[{type:'text',text:JSON.stringify(safe)}]})}if(msg.id!==undefined)return reply(msg.id,null,{code:-32601,message:'method not found'})}
function reply(id,result,error){process.stdout.write(JSON.stringify({jsonrpc:'2.0',id,...(error?{error}:{result})})+'\n')}
async function state(){const base=process.env.CWD_DATA_DIR||(process.env.XDG_DATA_HOME||path.join(os.homedir(),'.local','share'))+'/codex-worker-delegation';try{return JSON.parse(await fs.readFile(path.join(base,'state.json'),'utf8'))}catch{return{mode:'AUTO',mainSource:'official',models:{},provider:null,protocolCache:{}}}}
